import styled from 'styled-components';

const Wrapper = styled.div`
	width: 50%;
	display: flex;
	justify-content: space-between;
`;

const ColorComponentWrapper = styled.div`
	display: flex;
	flex: 1;
	flex-direction: column;
	align-items: center;
`;

export {Wrapper, ColorComponentWrapper};
